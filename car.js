/* --- Строка пробега --- */
.car-mileage-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 4px;
}

.car-mileage-label {
  font-size: 14px;
  color: var(--text-secondary);
}

.car-mileage-value {
  font-size: 16px;
  font-weight: 700;
  color: #fff;
}

/* --- Блок замены масла --- */
.oil-info-title {
  font-weight: 600;
  margin-bottom: 8px;
  color: #fff;
}

.oil-info-row {
  display: flex;
  gap: 16px;
  font-size: 14px;
  color: var(--text);
}

.oil-info-type {
  font-size: 14px;
  color: var(--text);
  margin-top: 4px;
}

.oil-info-note {
  font-size: 12px;
  color: var(--text-secondary);
  margin-top: 4px;
}

.oil-note-full {
  font-size: 11px;
  color: var(--text-secondary);
  width: 100%;
}
